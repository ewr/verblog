- register_content assets.first
.pf.pfR{ :style => "width: #{ assets.first.asset.modal.width }px"}
	= assets.first.asset.modal.tag
	.pfP= assets.first.asset.owner
	= markdown(assets.first.caption).html_safe